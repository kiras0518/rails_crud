<%= search_form_for @q do |f| %>
<div class="col-md-4">
        <div class="input-group">
            <%= f.search_field :title_cont ,class: 'form-control'%>
              <span class="input-group-btn">
                <%= f.submit class: 'btn btn-default' %>
              </span>
        
        </div>
    </div>
    
            <div class="dropdown">
                    <button class="btn btn-default dropdown-toggle" type="button" data-toggle="dropdown">狀態搜尋
                    <span class="caret"></span></button>
                    <ul class="dropdown-menu">
                      <li><%= sort_link @q, :spending_in, "完成" %></a></li>
                      <li><%= sort_link @q, :handling, "進行中" %></a></li>
                      <li><%= sort_link @q, :finished, "未完成" %></a></li>        
                    </ul>
            </div>
    
            <%= f.select :status_in, Task.statuses.to_a.map { |w| [w[0].humanize, w[1]] },{include_blank: true}, {multiple: true} %> 

<% end %>
